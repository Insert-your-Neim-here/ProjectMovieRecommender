services:
  db:
    image: pgvector/pgvector:pg16
    environment:
      POSTGRES_DB: moviejournal
      POSTGRES_USER: mj
      POSTGRES_PASSWORD: mj
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata: